# Normalize line endings: store all text as LF in the repo (checked out native on
# Windows), so the cross-platform tree stays consistent and diffs stay clean.
* text=auto eol=lf

# Lockfiles and vendored assets are text too; keep them LF-normalized.
*.lock text eol=lf
*.js   text eol=lf
*.css  text eol=lf
